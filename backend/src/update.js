// Deprecated update module stub.
// Original implementation moved to `src/legacy/main-ui/update.js` during refactor.
// Keep a minimal stub to avoid breaking imports. Remove when renderer fully migrated.

console.warn(
  '[update.js] Legacy update script stub loaded. See src/legacy/main-ui/update.js for original code.'
);

export default function update() {
  return Promise.resolve(false);
}
